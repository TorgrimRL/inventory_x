# Repository and CI setup – 2026-01-14

## Purpose

The purpose of this meeting was to review early setup progress and make sure the repository was ready for team development.

## Summary

The team had made significant progress on the initial technical setup. The React frontend had been set up with Vite and TypeScript and connected to the backend for retrieving inventory data. The Django backend had an inventory model, database connection, seeded test data, and tests. PostgreSQL was in use, and the stack could run through Docker Compose. GitHub Actions had also been configured to run automated checks and tests for both frontend and backend.

The repository was prepared for team collaboration with issue templates, a pull request template, ignore files, and Makefile commands for running, formatting, and testing the project.

## Completed setup work

- React frontend set up with Vite and TypeScript.
- Frontend connected to backend for retrieving inventory data.
- Django backend set up with an inventory model.
- Database connection and seeded test data added.
- Backend tests added.
- PostgreSQL taken into use.
- Docker Compose configured for backend, frontend, and database.
- GitHub Actions configured for automated frontend and backend checks.
- Repository prepared with issue templates, pull request template, ignore files, and Makefile commands.

## Board update

The GitHub Project board was still being prepared as part of the early setup. No board screenshot was captured for this meeting.

## Action items

- Continue refining the backlog.
- Continue turning user stories into implementation tasks.
- Use the repository workflow for further development.
